import numpy as np

np.random.seed(42) # seed fixes an starting point for generating random numbers, so that the same sequence of random numbers is produced every time the code is run
print(np.random.randint(1, 10, 5)) #it ensures to give same random number everytime
